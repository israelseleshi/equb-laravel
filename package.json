{
  "name": "equb",
  "private": true,
  "scripts": {
    "start:all": "npx -y concurrently -n \"mobile,backend\" -c \"cyan,green\" \"npm start --prefix mobile-app\" \"php -S localhost:8000 -t backend/public\""
  }
}
